/**
 * gradlew war コマンド実行時の処理の設定
 */

apply {
    plugin 'war'
}

// TODO 要確認：倉庫番のリリーススクリプト等の関係上、現状は固定してる、だったと思う。。
ext {
    warVersion = '1.0'
}

war {
    baseName = "${projectName}"
    version = "${warVersion}"
}